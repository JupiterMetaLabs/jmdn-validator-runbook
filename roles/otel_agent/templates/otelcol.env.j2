# Managed by jmdn-validator-runbook. Mode 0640, group-readable by {{ otel_agent_group }}.
#
# The token lives here and nowhere else: not in config.yaml, not in the systemd
# unit, not in argv. `ps` will never show it.
JMDT_OTEL_TOKEN={{ telemetry.token }}
